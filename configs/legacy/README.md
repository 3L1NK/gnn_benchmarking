# Legacy Configs

This directory stores historical config files for reference.

- These files are not the primary runnable entry points.
- Use `configs/runs/*` for current experiments.
- Old paths are mapped to current paths by `utils/config_aliases.py`.
